plugins { `kotlin-dsl` }

group = "com.kouqurong.buildlogic"

// Configure the build-logic plugins to target JDK 17
// This matches the JDK used to build the project, and is not related to what is running on device.

dependencies {}

gradlePlugin { plugins {} }
